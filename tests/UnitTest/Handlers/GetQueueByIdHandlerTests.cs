using MediatR;
using QueueProject.Application.Common.Interfaces;
using QueueProject.Application.Queues.Queries.GetQueueById;
using QueueProject.Domain.Entities;
public class GetQueueByIdHandlerTests 
{
    //private readonly IQueueRepository _repo;

    //public GetQueueByIdHandlerTests(IQueueRepository repo)
    //{
    //    _repo = repo;
    //}

    //public async Task<Queue?> Handle(GetQueueById request, CancellationToken cancellationToken)
    //{
    //    return await _repo.GetByIdAsync(request.Id);
    //}
}

